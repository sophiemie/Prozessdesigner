unit UNodeTest;

interface

uses
  DUnitX.TestFramework, UNodes;

type
  [TestFixture]
  TStartTestObject = class
  strict private
    aStartNode : TStart;
  public
    [Setup]
    procedure Setup;
    [TearDown]
    procedure TearDown;
    [TestCase('Test setze Start-ID','5')]
    procedure setID(newID : Integer);
    [Testcase('Test gebe Start-ID','1')]
    procedure getID(_Result: Integer);
    []
    procedure setDes
  end;

implementation

procedure TStartTestObject.Setup;
begin
  aStartNode := TStart.Create(1);
  aStartNode.setDescription('Neuer Prozess');
end;

procedure TStartTestObject.TearDown;
begin
end;

{ Es wird die neue ID erwartet, die gesetzt wurde }
procedure TStartTestObject.setID(newID : Integer);
var
  R : Integer;
begin
  aStartNode.setID(newID);
  R := aStartNode.getID;
  Assert.AreEqual(R,newID);
end;

{ Es wird die ID erwartet, die bei der Initialisierung uebergeben wurde }
procedure TStartTestObject.getID(_Result: Integer);
var
  R : Integer;
begin
  R := aStartNode.getID;
  Assert.AreEqual(R,_Result);
end;


initialization
  TDUnitX.RegisterTestFixture(TStartTestObject);

end.
